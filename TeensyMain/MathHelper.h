#include <Arduino.h>
float calculateAdjustedMean(float[], float, float);
float calculateMean(float[]);
float calculateSD(float[], float);
float filteredMean(float[]);
float euclideanDist(int x1, int x2, int y1, int y2);
